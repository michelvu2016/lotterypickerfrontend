import { NumberDecorDirective } from './number-decor.directive';

describe('NumberDecorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberDecorDirective();
    expect(directive).toBeTruthy();
  });
});
